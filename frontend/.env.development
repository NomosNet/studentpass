# Тестовая роль без входа: admin | manager | user | off
# В прод-сборке игнорируется (.env.development не подключается)
VITE_DEV_AUTH_ROLE=admin

# Бэкенд в Docker (backend/docker-compose.yml)
VITE_API_BASE_URL=http://localhost:8080
