# Скопируйте в .env.development при необходимости
# Тестовая роль (dev): admin | manager | user | off
VITE_DEV_AUTH_ROLE=off
# Docker: docker compose up в backend/ → порт 8080
VITE_API_BASE_URL=http://localhost:8080
